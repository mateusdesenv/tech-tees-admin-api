{
  "name": "tech-tees-admin-api",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --import tsx src/server.ts",
    "dev:watch": "node --env-file=.env --watch --import tsx src/server.ts",
    "build": "tsc",
    "start": "node dist/src/server.js",
    "test": "node --test --import tsx \"test/**/*.test.ts\"",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "mongodb": "^6.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
