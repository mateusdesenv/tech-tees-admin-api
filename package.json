{
  "name": "tech-tees-admin-api",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env scripts/local-api.mjs",
    "dev:watch": "node --env-file=.env --watch scripts/local-api.mjs",
    "build": "tsc",
    "start": "node scripts/local-api.mjs",
    "test": "node --test --import tsx \"test/**/*.test.ts\"",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "mongodb": "^6.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
